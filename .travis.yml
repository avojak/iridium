---

language: node_js

node_js:
  - lts/*

sudo: required

services:
  - docker

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - libstdc++-5-dev
      - libgee-0.8-dev
      - libgee-0.8-2

install:
  - npm i -g @elementaryos/houston

script:
  - houston ci
